# Walkthrough Video

Status: placeholder. Video will be recorded once the core workflow (Day 2) and
completion/admin/polish (Day 3) work is finished, per the brief's minimum demonstration
scenario.

**Video link:** _to be added before submission_

## Planned content (per the official brief's video requirements)

- What was built and why
- Technology stack
- Application architecture (see `docs/architecture.md`)
- Requester workflow: create → submit a reimbursement request
- A validation error, shown live
- Reviewer workflow: view → approve / reject (with reason) → mark Paid
- Search and filtering
- Dashboard totals
- Role-based access control (employee / reviewer / admin boundaries)
- How data is persisted (Supabase Postgres via Prisma)
- How receipts are protected (private bucket, signed URLs, authorized access only)
- Key tests performed (see `docs/testing.md`)
- Known limitations and what would be improved next (see `docs/reflection.md`)

Target length: 3–5 minutes, per the brief.
